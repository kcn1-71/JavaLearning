import java.util.Hashtable;
import java.util.Map;

public class StringSearch {
	
	
	public static Map<Character, Integer> ArrayOfShifts = new Hashtable<Character, Integer>();
		
	public static void ShiftForm(String S)
	{
		for (int i = S.length()-2, j=S.length(); i >= 0; i--, j--)
        {
            if (!ArrayOfShifts.containsKey(S.charAt(i)))
            {
            	ArrayOfShifts.put(S.charAt(i), S.length()-j);
            }
        }
	}

	public static void main(String[] args) {
		
		ShiftForm("rqwer");
	}

}
